# The Toggle Project

Bringing every Tracker condition up to **three complete reading registers** — Brief, Standard and Detailed — and giving every genuine condition a **Complications** row.

Units of work are called **sets**, one per specialty. (Deliberately not "batches" — that word belongs to the Notebook project.)

---

## The target state for one condition

```js
{
  id: "endo_addisons",
  depth: "detailed",          // the base fields below are the DETAILED register
  standard: { …8 fields },    // ~190–230 chars/field, HFpEF register
  brief:    { …4 core fields },// ~110 chars/field
  complications: "…",         // required on genuine conditions
  keyFacts: "…", epidemiology: "…", …   // the base = detailed, ~480 chars/field
}
```

**Every condition ends at `depth: "detailed"`** with authored `standard` and `brief` alongside. That is what Neurology looks like now and it is the pattern for the whole project.

Registers, measured from the finished Neurology set:

| Register | Fields | Chars/field | Measured band |
| --- | --- | ---: | --- |
| Brief | core 4 | ~110 | — |
| Standard | 7 base fields | **~200** | **150–230, enforced by script** |
| Detailed | 7 base fields | ~480 | — |

Standard is measured over the seven base fields **excluding complications**, so it compares directly with the HFpEF reference of 192 chars/field.

**Topic-style entries are excluded from Complications** — they are processes, concepts and drugs, not diseases. There are 49: `pharmacology` (23), `public_health` (10), `reproductive` (4), `paediatrics` (4), plus `psych_risk`, `haem_anticoag`, `id_notifiable`, `id_abx_choice`, `immuno_hypersensitivity`, `immuno_allergy_testing`, `genetics_inheritance`, `genetics_counselling`. They may still gain registers.

---

## Set order

Sets 1–3 were chosen by the user. The rest are ordered by exam weight first, then by size, so the highest-yield material is finished earliest.

| Set | Specialty | Genuine conditions | Need complications | At brief base | Status |
| ---: | --- | ---: | ---: | ---: | --- |
| 0 | Neurology | 45 | 0 | 0 | ✅ done 2026-08-09 |
| 1 | Endocrinology & Metabolic | 42 | 36 | 26 | ✅ done 2026-08-09 |
| 2 | Cardiovascular | 63 | 62 | 48 | ✅ done 2026-08-10 |
| 3 | Gastroenterology & Nutrition | 53 | 44 | 36 | ✅ done 2026-08-11 |
| 4 | Respiratory | 27 | 27 | 16 | ✅ done 2026-08-11 |
| 5 | Infectious Diseases | 37 | 36 | 35 | ✅ done 2026-08-11 |
| **6** | **Dermatology** | **36** | **36** | **36** | **next** |
| 7 | Renal & Urology | 27 | 26 | 24 | pending |
| 8 | MSK & Rheumatology | 28 | 28 | 27 | pending |
| 9 | Paediatrics & Child Health | 23 | 23 | 20 | pending |
| 10 | Reproductive & Sexual Health | 22 | 22 | 21 | pending |
| 11 | Haematology | 19 | 19 | 19 | pending |
| 12 | Ophthalmology | 25 | 24 | 24 | pending |
| 13 | ENT | 18 | 18 | 18 | pending |
| 14 | Genetics | 19 | 16 | 16 | pending |
| 15 | Psychiatry & Mental Health | 15 | 15 | 15 | pending |
| 16 | Immunology & Allergy | 9 | 9 | 8 | pending |
| 17 | Pharmacology + Public Health | 0 | 0 | — | registers only, no complications — decide later whether worth doing |

Sets 0–4 ran in the order above. **On 2026-08-11 the user re-ordered what follows: Infectious Diseases next, then Dermatology, with everything else keeping its previous relative order.** That brings the two largest remaining sets forward — 37 conditions each, and both almost entirely at brief base (35/37 and 37/37), so they are the heaviest writing left in the project. Doing them now trades the quick conversions for getting the bulk out of the way while momentum is high; Renal and Rheumatology, which are mid-sized and partly written already, move back behind them. The small specialist sets stay last, as quick wins that can absorb whatever time remains.

**⚠️ Dermatology moving to Set 6 brings a shared line forward: `derm_ichthyosis` + `derm_neuropathic_ulcer` must be split with `scratchpad/split_line.py` before the injector runs on that cluster.** See the script-safety rules below.

**Counts re-verified against `data.js` on 2026-08-11**, since the next two sets are planned against them. Infectious Diseases and Renal were correct as listed. **Dermatology was wrong — it is 36 conditions, not 37** (all 36 genuine, all needing complications, all at brief base): the original figure predates conditions moving in and out of that specialty, with `derm_shingles` going to Infectious and `derm_leg_ulcer` to Cardiovascular while `cvs_gangrene` came the other way. Treat the other pending rows as approximate for the same reason and re-count at the start of each set, as step 1 already requires.

---

## How to run a set

1. **List the specialty** with cluster, `depth`, chars/field and whether `complications` exists.
2. **Work cluster by cluster**, not alphabetically — clinical neighbours share content and it keeps contrasts sharp (e.g. the thyroid causes in uptake order).
3. For each condition produce, in one pass: **detailed base**, **standard**, **brief**, **complications**.
   - Where the base is already brief-register, the existing text usually survives almost intact as the `brief` object — reuse it rather than rewriting.
   - Where the base is already detailed, only `standard` and `brief` are needed.
4. **Inject by script, never by hand.** `scratchpad/apply_registers.py` does the whole job from one JSON payload: it rewrites the base to detailed (or keeps it, if the payload omits `base`), flips `depth`, and inserts `standard`, `brief` and `complications`, splicing `management` back as the **raw source substring** so flowcharts stay byte-identical. Run it with `--dry-run` first. (It replaces the earlier pair `inject_registers.py` / `upgrade_condition.py`, whose behaviour it subsumes.)
   - Before the injector, run **`measure_payload.py`**: it reports every band for the whole payload *without aborting on the first failure*, so an over-long batch is tuned in one pass instead of one condition per run. `apply_registers.py` is still the gate.
   - Mark carried-through complications `"__EXISTING__"` in the payload and run **`resolve_existing.py`**, which substitutes the value already in `data.js`. Retyping good text from an earlier pass is how typos get in.
   - When the ceiling fires, write the trims by hand into a trims file and merge with **`merge_trims.py`**, which refuses any replacement that is not strictly shorter. Run **`prune_trims.py`** first on a large trims file: it drops only the fields that were pasted back unchanged (printing each one) so the batch is not aborted one no-op at a time, and leaves every genuine trim to face the unchanged `merge_trims.py` gate.
5. **Verify**: balance `0/0/0`, `node --check`, unique ids, zero duplicate top-level keys, every `standard` complete at 8 fields and every `brief` at 4, then a live check in the app.
6. **Update** this file's status column and `content-status.md`, then give the re-upload list.

### Script safety rules (learned the hard way — see the memory file)

- Assert **before** opening the file for writing, so a failed check leaves it untouched.
- Scan the **whole** condition object for a key, not just the part before `cluster:`, and assert the key appears exactly once.
- For pure reorders, assert the id multiset **and** total byte length are unchanged.
- Assert the **direction**: a script whose job is to expand must refuse a payload shorter than what it replaces (and vice versa for a shortening script). This applies to the trim files too — when an over-long batch has to be tightened by hand, run the trims through a merge script that asserts every replaced field is shorter, then re-checks the band.
- **Enforce both ends of the detailed band.** `depth: "detailed"` is a label, not evidence: cardiac tamponade carried it at 297 chars/field. The 330 floor caught it.
- **⚠️ Some lines of `data.js` hold TWO condition objects each**, so whole-line replacement would silently delete the sibling. `apply_registers.py` refuses any line containing more than one `id: "`, and `scratchpad/split_line.py` splits one safely. Three pairs remain (verified 2026-08-11), and the note that they are "all in future sets" was WRONG: **`neuro_trigeminal` + `neuro_iih` and `neuro_status_epilepticus` + `neuro_parkinsons` are ALREADY-COMPLETE Set 0 conditions** that still sit on shared lines, because Neurology was written before `apply_registers.py` existed. They matter only if those entries are revisited — which they may be, since `neuro_dcm`, `neuro_neurogenic_shock` and `neuro_autonomic_dysreflexia` are all on the out-of-band list. The one genuinely ahead is **`derm_ichthyosis` + `derm_neuropathic_ulcer`, now in Set 6** after the re-order. (`cvs_arterial_ulcer` + `cvs_ali` was split on 2026-08-10.)
- **⚠️ Never enumerate a specialty by an id prefix.** Ids are preserved through splits and moves, so a condition can sit in one specialty carrying another's prefix: `haem_wilson` lives in Gastroenterology's **Liver disease** cluster, and `derm_shingles` lives in Infectious Diseases' **Viral exanthems & herpesviruses** cluster (written in Set 5, so it must not be counted again in Set 6). A `id: "gi_` scan reports the liver cluster as 9 conditions when it is 10. **Always list by `cluster:`**, and cross-check the count against the specialty's `conditions` array length.
- **⚠️ Do not assume `management` sits between `depth` and `cluster`** — five entries store it LAST, after `treatment`: `cvs_ruptured_aaa` (done), and `gi_ugib`, `gi_cholecystitis`, `gi_cholangitis` and `repro_afe`, which are still to come. Locate its value by brace-matching from the key, never by position relative to `cluster`. Re-emitting the object normalises it back to the canonical order, which is fine — but a script that slices by position will mangle it.

---

## ✅ Standard-register drift — found and corrected (2026-08-09)

The brief specifies Standard as "generally the same amount of text as in the HFpEF condition", i.e. **192 chars/field, 1,345 total**. The register drifted upward set by set until it was caught by measurement:

| Set | Before | After |
| --- | ---: | ---: |
| Neurology | 229 | **217** |
| Endocrinology | 239 | **217** |
| Cardiovascular (first 16) | 298 | **217** |
| **All 103** | 249 | **217** — range 183–230, **0 outside band** |

The clearest symptom was that **HFpEF's own authored Standard had reached 266 chars/field**, when its original text at 192 literally defined the target; it is now 230. Brief (107) and Detailed (457) were on spec throughout — the drift was confined to Standard.

**Guard now in place.** `apply_registers.py` measures Standard over the **7 base fields** (excluding complications, so it is directly comparable to the HFpEF reference) and **aborts** outside **150–230** — as of 2026-08-10 the band is enforced, not merely warned. The previous bands were 120–300 and 140–300, wide enough to wave 298 through unremarked.

**⚠️ A mechanical trim was tried and rejected — do not repeat it.** Auto-dropping trailing sentences from over-long fields generated 264 candidates, but review showed **124 of them removed critical content**: the *"correct by NO MORE than 10 mmol/L in 24 hours"* rule from SIADH, *"ANTITHYROID DRUGS ARE NOT INDICATED"* from De Quervain's, *"do NOT biopsy"* from phaeochromocytoma, and the familial hypocalciuric hypercalcaemia *"must not be operated on"* warning. These fields end with their punchline, which is exactly what a tail-trim removes. **Tighten by hand.**

`patch_standard.py` was written for this job: it replaces individual fields inside a `standard` object, leaves the base text, `brief`, `management` and everything else untouched, and **refuses any replacement that is not shorter than what it replaces** — which repeatedly caught fields pasted back unchanged.

## Progress log

- **2026-08-11 — ✅ SET 5 (INFECTIOUS DISEASES) COMPLETE, 37 of 37, in a single session.** All eight clusters done in nine passes: **Sepsis & bacteraemia** (4), **CNS infection** (4), **Respiratory infection** (5), **Gastrointestinal infection** (6), **Sexually transmitted infection** (3), **Viral exanthems & herpesviruses** (7, split into a childhood-exanthem pass and a herpesvirus pass) and **Tropical, zoonotic & other infection** (8, split in two).
  - **All 37 needed their detailed text written from scratch** — every one sat below the 330 floor. Only two were above brief register to begin with: `id_sbp` at 186 c/f (labelled `standard`) and `derm_shingles` at 185. `id_sbp` was also the only entry with an existing `complications` row, carried through verbatim with `resolve_existing.py` rather than retyped.
  - Registers for the 37: brief **103** (95–116), standard **207** (189–220), detailed **612** (556–639), **0 outside any band**. App-wide, **267 conditions are now complete** at brief 103 / standard 211 / detailed 538.
  - **Budgeting Standard at ~200 rather than "just under 230" worked.** Acting on the Set 4 warning, each pass was drafted to ~200; the set finished at **207 against Respiratory's 223**. The upward pressure is still real — passes A, C and F2 first drafted at 216–231 — but it was caught and trimmed before injection every time, so nothing shipped near the ceiling.
  - **The detailed ceiling fired on 13 of the 37**, and the pattern held exactly: the dense multi-mechanism topics overshoot. TB (656), typhoid (642), C. difficile (659), HIV (666), syphilis (657), gonorrhoea (653), CMV (688 — the worst of the set), shingles (659), leishmaniasis (669), and all four of the final pass — schistosomiasis (652), rabies (658), tetanus (666) and invasive fungal infection (651, which needed a second trim round). All were tightened **by hand** through `merge_trims.py`; no trailing-sentence auto-trim was used. The two passes drafted deliberately shorter (CNS infection; measles/rubella/coxsackie/molluscum) cleared every band on the **first** draft.
  - **⚠️ Never enumerate this specialty by id prefix — the rule bit again in reverse.** `derm_shingles` lives in Infectious Diseases' **Viral exanthems & herpesviruses** cluster carrying its dermatology id, so it was written as part of Set 5 and must NOT be counted again in Set 6. Conversely `id_sbp`, a liver condition, sits in the **Gastrointestinal infection** cluster here.
  - **A new helper was added: `scratchpad/prune_trims.py`.** `merge_trims.py` aborts the entire batch on the first replacement that is not strictly shorter, which is the right gate (it is exactly how text pasted back unchanged gets caught, and it fired five times this set). But in a large hand-written trims file that cost one round-trip per no-op field. `prune_trims.py` deletes only the no-op entries, prints what it dropped, and leaves everything else to go through the unchanged `merge_trims.py` gate. **It never edits text — it only removes keys**, so a dropped field simply keeps the payload value.
  - Verified: balance 0/0/0, `node --check`, 558 unique ids with the set unchanged, **0 duplicate keys across all 1,520 object literals** (all nesting levels, string-aware), **all 181 flowcharts re-rendered through the Mermaid engine with 0 failures, 0 blank tooltips, 0 orphan tooltips and every rendered node carrying a tip**, **0 conditions changed outside the 37**, 0 field defects. Live check: Bacterial meningitis renders 8 authored rows at Detailed (5,506 chars) with its flowchart drawn in the card (8 nodes, every fork label rendering) and 4 rows at Brief (1,128 chars); no fallback markers.
  - **⚠️ Flowchart byte-identity was NOT diffed against a baseline this set** — no pre-edit flowchart snapshot was taken, and the scratchpad had been cleaned of any earlier one. What guarantees it is `apply_registers.py`'s write-time assertion that the raw `management` substring survives verbatim in the rewritten line, applied to the five set-5 conditions carrying charts (`id_sepsis`, `id_meningitis`, `id_cdiff`, `id_sbp`, `id_malaria`), plus the hash diff showing nothing else changed. **Snapshot the flowcharts before the next set** so the direct diff is available again.
  - **Not part of this set:** the two topic-style entries `id_notifiable` and `id_abx_choice` (33 c/f each) remain at brief base, excluded from Complications by the topic rule but still eligible for registers — decide with Set 17. The six pre-band conditions from Sets 0–2 remain outside the detailed band and still need a decision.
- **2026-08-11 — ✅ SET 4 (RESPIRATORY) COMPLETE, 27 of 27, in a single session.** All six clusters done in five passes: **Airways disease** (7, split into an asthma group and a COPD group), **Respiratory infection** (6), **Interstitial & granulomatous disease** (3) with **Pneumoconiosis** (3), then **Lung cancer & pleural disease** (5) with **Sleep-disordered breathing & critical care** (3).
  - **All 27 needed their detailed text written from scratch** - every single one sat below the 330 floor, including the nine already labelled `standard` and `resp_asthma_acute`, which was flagged `detailed` at only 315 c/f. None had a `complications` row. This was the first set where not one condition kept its base.
  - Registers: brief **97**, standard **223** (206-230, 0 outside band), detailed **617** (580-640, 0 outside). App-wide, **230 conditions are now complete** at brief 103 / standard 211 / detailed 526.
  - **⚠️ STANDARD DRIFTED UPWARD AGAIN, and this set is the clearest warning since the original correction.** Respiratory finished at **223 chars/field against gastro's 209** and the 192 HFpEF reference, and **17 of 27 conditions breached the 230 ceiling on first draft** - the whole of pass D came in at 231-241 and pass E at 228-246. Every one was caught by the enforced band and trimmed, so nothing shipped out of spec, but the pressure is clearly systematic rather than accidental: Standard creeps whenever the detailed text is dense, because the summariser keeps more of it. **Budget Standard at ~200, not "just under 230".**
  - Two mechanical points worth recording. **The brief FLOOR fired three times** (`resp_copd` 83, `resp_uri` 79, `resp_effusion` 83) - the first floor breaches of the project. `merge_trims.py` cannot fix these, because it only shortens; the payload has to be edited directly to LENGTHEN the field. **And the drafting budget worked again**: the two passes drafted deliberately at ~520 (asthma, biliary-style structural topics) cleared on the first attempt, while the dense multi-mechanism passes did not.
  - Verified: balance 0/0/0, `node --check`, 558 unique ids with the set unchanged, **0 duplicate keys across all 1,446 object literals**, **all 181 flowcharts byte-identical AND all 181 re-rendered through the Mermaid engine with 0 failures and 0 orphan/blank tooltips**, **0 conditions changed outside the 27**, 0 field defects. Live check: Acute asthma exacerbation renders 8 authored rows at Detailed (6,512 chars) with its flowchart drawn in the card (8 nodes); no fallback markers.
- **2026-08-11 — ✅ SET 3 (GASTROENTEROLOGY & NUTRITION) COMPLETE, 53 of 53.** The last four clusters landed in four passes: **Biliary tract & gallbladder** (7), **Pancreas** (3), **Neuroendocrine tumours** (2) and **Nutrition & metabolic** (2). All 14 needed their detailed text written from scratch — every one sat below the 330 floor, including four already labelled `standard` (both carcinoid entries, and both lipid entries at 247–268 c/f).
  - Registers for these 14: brief **101**, standard **219** (207–228, 0 outside band), detailed **612**. **The finished specialty reads brief 99 / standard 209 (189–228) / detailed 582 across all 53, with 0 outside any band.** App-wide, **203 conditions are now complete** at brief 104 / standard 210 / detailed 514.
  - **`gi_cholecystitis` and `gi_cholangitis` — the last two `management`-after-`treatment` entries in the file — went through without incident.** The brace-matching injector preserved both flowchart blobs byte-identically (1,800 and 2,166 chars), and the cholangitis flowchart was then confirmed to still render *in the card* (10 nodes), not merely to match on disk. **Only `repro_afe` (Set 10) now retains that key order.**
  - **The detailed ceiling fired six times again, and the pattern from the liver pass held exactly**: the four passes that overshot were the two carcinoid entries (669, 658) and the two lipid entries (665, 673) — all dense, multi-mechanism topics. The two lipid entries also breached the **standard** ceiling (235, 231), the first time this set that Standard needed trimming. All were tightened by hand through `merge_trims.py`. **Meanwhile the three passes drafted deliberately at ~520 (biliary acute, biliary chronic/malignant, pancreas) all cleared both bands on the FIRST draft** — 10 of 14 conditions needed no trimming at all, against 0 of 21 in the previous two clusters. Drafting to the budget rather than trimming to it is measurably cheaper.
  - Verified: balance 0/0/0, `node --check`, 558 unique ids with the id set unchanged, **0 duplicate keys across all 1,392 object literals** (all nesting levels, string-aware), **all 181 flowcharts byte-identical AND all 181 re-rendered through the Mermaid engine with 0 failures and 0 orphan/blank tooltips**, **0 conditions changed outside the 14**, 0 field defects. Live check: Ascending cholangitis renders 8 authored rows at Detailed (5,605 chars) with its flowchart drawn in the card; no fallback markers.
  - **Still outstanding and NOT part of this set**: the six pre-band conditions from Sets 0–2 that sit outside the detailed band — `neuro_dcm` (718), `cvs_hypotension` (320), `endo_de_quervain_thyroiditis` (315), `endo_kallmann` (285), `neuro_neurogenic_shock` (265), `neuro_autonomic_dysreflexia` (259). Four were consciously kept at Set 0/1 time, so this needs a decision.
- **2026-08-10 — Set 3 (Gastroenterology & Nutrition) continued, now 39 of 53.** Two more whole clusters complete: **Lower GI & colorectal** (11) and **Liver disease** (10). Worked in six passes — acute lower GI (appendicitis, diverticular disease, acute mesenteric ischaemia, ischaemic colitis), hernias and colorectal (inguinal, femoral, Peutz-Jeghers, colorectal cancer), anorectal (haemorrhoids, anal fissure, anal fistula), the hepatitis group (viral, autoimmune, alcoholic), MASLD/cirrhosis/encephalopathy, then Wilson's, Budd-Chiari, liver abscess and HCC.
  - **18 of the 21 needed their detailed text written from scratch.** Only three kept their base: `gi_inguinal_hernia` (386 c/f), `gi_femoral_hernia` (382) and `gi_masld` (351), all already in band. **Two entries whose `depth` label overstated the register were caught by the 330 floor and expanded**: `gi_ischaemic_colitis`, flagged `detailed` at only 286 c/f, and `gi_acute_mesenteric_ischaemia` at 277 (flagged `standard`). That is now the fourth and fifth time a `depth` label has proved not to be evidence.
  - Registers for these 21: brief **98** (90–106), standard **210** (195–221, **0 outside band**), detailed **574** (351–640, 0 outside). **Gastroenterology as a whole now reads brief 98 / standard 205 (189–221) / detailed 573.** App-wide, **189 conditions are complete**, at brief 104 / standard 209 (183–230, 0 outside band) / detailed 507.
  - **⚠️ `haem_wilson` lives in the Liver disease cluster with its original haematology id** — the split/move convention preserving the id. Any script that finds gastro conditions by an `id: "gi_` prefix will silently miss it, and did on the first listing pass here; scan by `cluster:` instead. The cluster is 10, not the 9 a prefix scan reports.
  - **The detailed ceiling fired six times** — `gi_hepatitis` (657), `gi_alcoholic_liver` (691), `gi_cirrhosis` (661), `gi_hepatic_encephalopathy` (652), `gi_liver_abscess` (665) and `gi_hcc` (672). All were tightened BY HAND through trim files merged by a new `scratchpad/merge_trims.py`, which asserts every replacement is strictly shorter than what it replaces. No trailing-sentence auto-trim was used. **The liver clusters overshoot hardest** — six of the six ceiling hits were liver entries, so budget nearer 520 c/f there than 570.
  - **Three helper scripts were added** and are worth keeping: `measure_payload.py` (reports every band for a whole payload without aborting on the first failure, so a batch is tuned in one pass rather than N), `resolve_existing.py` (substitutes `"__EXISTING__"` complications from `data.js`, so good text from an earlier pass is carried through verbatim instead of retyped — used on 6 entries here) and `merge_trims.py`. `snapshot_conditions.py` hashes every condition object so a later run can prove exactly which ones changed.
  - Remaining clusters: **Biliary tract & gallbladder (7)**, Pancreas (3), Neuroendocrine tumours (2), Nutrition & metabolic (2). ⚠️ The biliary cluster still holds `gi_cholecystitis` and `gi_cholangitis`, the two remaining gastro entries that store `management` LAST.
  - **A pre-existing defect was found outside this scope and is NOT fixed**: six conditions from Sets 0–2, all written before the band was enforced on 2026-08-10, sit outside the detailed band — `neuro_dcm` (718, above the ceiling), `endo_kallmann` (285), `neuro_autonomic_dysreflexia` (259), `neuro_neurogenic_shock` (265), `endo_de_quervain_thyroiditis` (315) and `cvs_hypotension` (320). The last four were explicitly reviewed and kept at Set 0/1 time, so a decision is needed rather than a blind rewrite.
  - Verified: balance 0/0/0, `node --check`, 558 unique ids with the id set unchanged, **0 duplicate top-level keys across all 1,364 object literals** (all nesting levels, string-aware), **all 181 flowcharts byte-identical AND all 181 re-rendered through the Mermaid engine with 0 failures and 0 orphan/blank tooltips**, **0 conditions changed outside the 21**, 0 field defects. Live check in the running app: Peutz-Jeghers renders 8 authored rows at Detailed (5,415 chars) and 4 at Brief (755); Wilson's disease renders 8 at Standard (2,125) under the Liver disease cluster; no fallback markers.
- **2026-08-10 — Set 3 (Gastroenterology & Nutrition) STARTED, 18 of 53.** Four whole clusters complete: **Oesophagus & upper GI** (8), **Stomach & peptic disease** (5), **Small bowel & malabsorption** (2) and **Inflammatory bowel disease & functional** (3). 15 of the 18 needed their detailed text written from scratch; **`gi_barretts`, `gi_oesophageal_ca` and `gi_variceal_haemorrhage` were already at detailed register and in band, so they kept their base** and took only the shorter registers plus complications.
  - Registers for these 18: brief **99**, standard **199** (range 189–209, 0 outside band), detailed **571** chars/field. App-wide, 168 conditions are now complete.
  - **The `management`-after-`cluster` fix earned its keep immediately**: `gi_ugib` is one of the three gastro entries with that key order, and the brace-matching version handled it without incident. `gi_cholecystitis` and `gi_cholangitis` remain, in the still-unwritten Biliary cluster.
  - **The detailed ceiling fired three times** — `gi_ugib` (650), `gi_uc` (642, over by two characters) and `gi_ibs` (654). All were tightened by hand through trim files whose merge asserted every replaced field came back shorter, then re-checked the band.
  - Remaining clusters: **Lower GI & colorectal (11)**, Liver disease (10), Biliary tract & gallbladder (7), Pancreas (3), Neuroendocrine tumours (2), Nutrition & metabolic (2). Nine of these already carry complications from earlier work.
  - Verified: balance 0/0/0, `node --check`, 558 unique ids with the id set unchanged, **0 duplicate top-level keys across all 1,322 objects**, all 181 flowcharts unchanged, **0 conditions changed outside the 18**, 0 field defects. Live check: Coeliac disease renders 8 authored rows at Detailed (5,685 chars) and 4 at Brief (718), no fallback markers.
- **2026-08-10 — ✅ SET 2 (CARDIOVASCULAR) COMPLETE, 63 of 63.** The last three clusters landed in four passes: **Congenital heart disease** (5), **Aortic** (3), **Peripheral arterial** (5) and **Venous & thromboembolic** (4). All 17 needed detailed text written from scratch, including `cvs_pe`, which was flagged `detailed` at only 305 chars/field — the second entry this set whose `depth` label overstated its register.
  - Registers for these 17: brief **100**, standard **195** (range 189–201, 0 outside band), detailed **584**. **The finished specialty reads brief 104 / standard 201 / detailed 548 across all 63, with 0 outside the band.** App-wide, 150 conditions are now complete at brief 105 / standard 210 / detailed 489.
  - **Two structural problems were cleared before the content could go in**, both caught by assertions rather than discovered afterwards:
    1. **The `cvs_arterial_ulcer` + `cvs_ali` shared line was split** with `scratchpad/split_line.py`, which asserts the id multiset is unchanged and that the file grows by *exactly* `len(indent)` — proving only the line break moved. Its first run **aborted on a byte-count mismatch of one character**, which turned out to be my own arithmetic (the `, ` separator loses a space); the file was untouched, the formula was fixed, and it then ran clean.
    2. **`cvs_ruptured_aaa` stores `management` LAST, after `treatment`**, not between `depth` and `cluster`. The injector assumed the usual order and aborted. It now finds the management value by **brace-matching from wherever the key appears**, so key order no longer matters. **Four more entries file-wide share this quirk and sit in future sets: `gi_ugib`, `gi_cholecystitis`, `gi_cholangitis` (Set 3) and `repro_afe` (Set 10).**
  - Verified: balance 0/0/0, `node --check`, 558 unique ids with the id set unchanged, **0 duplicate top-level keys across all 1,286 objects**, **all 181 flowcharts identical in content AND all 181 re-rendered through the Mermaid engine with 0 failures**, **0 conditions changed outside the 17**, 0 field defects. Live check: Tetralogy of Fallot renders 8 authored rows at Detailed (5,218 chars), 8 at Standard (2,004) and 4 at Brief (712), no fallback markers.
- **2026-08-10 — Set 2 (Cardiovascular) continued, now 46 of 63.** Two more clusters complete: **Valvular heart disease** (9) and **Endocardial, myocardial & pericardial disease** (5). Worked in four passes — aortic valve (stenosis, sclerosis, regurgitation), mitral valve (stenosis, regurgitation, prolapse), right-sided valves plus rheumatic heart disease, then the endocardial/myocardial/pericardial group.
  - **All 14 needed their detailed text written from scratch.** Thirteen sat at brief register; the fourteenth, cardiac tamponade, was already flagged `depth: "detailed"` but measured only **297 chars/field** — below the 330 floor — so the enforced band caught it and its base was expanded to 570 as part of the pass. Worth remembering: a `depth` label is not evidence the register is right.
  - Registers for these 14: brief **103**, standard **196** (range 189–202, **0 outside band**), detailed **575** chars/field. Cardiovascular as a whole now reads brief 106 / standard 203 / detailed 534; all 133 finished conditions app-wide read brief 106 / standard 212 / detailed 477, still **0 outside band**.
  - **The enforced detailed ceiling did real work this pass.** All five of the endocardial/myocardial/pericardial entries were first drafted at 638–705 chars/field and the script refused the batch. They were tightened BY HAND through a separate trim file whose merge script asserted every field came back shorter — the same directional guard that `patch_standard.py` uses. No trailing-sentence auto-trim was used.
  - Remaining clusters, in order: **Congenital heart disease (5)**, Aortic & peripheral arterial (8), Venous & thromboembolic (4). Note the aortic/peripheral cluster contains the `cvs_arterial_ulcer` + `cvs_ali` shared line — split it before running the injector.
  - Verified: balance 0/0/0, `node --check`, 558 unique ids with the id set unchanged, **0 duplicate top-level keys across all 1,252 objects**, **all 181 flowcharts byte-identical**, **0 conditions changed outside the 14**, 0 field defects, and a live check in the running app of Aortic sclerosis (8 authored rows at Detailed, 3,986 chars; 8 at Standard, 1,926; 4 at Brief, 625) and Pericardial effusion (8 rows at Detailed, 5,064 chars), with no fallback markers.
- **2026-08-10 — Set 2 (Cardiovascular) continued, now 32 of 63.** The **Arrhythmias & conduction disease** cluster (16) is complete: all now carry a detailed base, an authored `standard`, an authored `brief` and a `complications` row. Worked in four passes by clinical grouping — atrial and supraventricular (AF, flutter, SVT, WPW), ventricular (VT, torsades, VF), bradycardia and conduction (bradycardia, heart block, RBBB, LBBB), then channelopathies, syncope and arrest (Brugada, long QT, vasovagal syncope, peri-arrest, cardiac arrest).
  - **12 of the 16 needed their detailed text written from scratch** (11 were at brief register, monomorphic VT at standard); 4 — torsades, VF, bradycardia and heart block — already had detailed bases and needed only the shorter registers plus complications.
  - Registers for these 16: brief **102**, standard **195** (range 184–205, **0 outside the 150–230 band**), detailed **522** chars/field. Cardiovascular as a whole now reads brief 107 / standard 206 / detailed 517; all 119 finished conditions app-wide read brief 106 / standard 214 / detailed 466, with **0 outside the band**.
  - Verified: balance 0/0/0, `node --check`, 558 unique ids with the id set unchanged, **0 duplicate top-level keys across all 1,224 objects** in the file, **all 181 flowcharts byte-identical**, **0 conditions changed outside the 16**, 0 field defects across the 16, and a live check of Wolff-Parkinson-White in the running app showing 8 authored rows at Detailed (5,006 chars), 8 at Standard (1,950) and 4 at Brief (669), with no fallback markers.
  - A new injector, `scratchpad/apply_registers.py`, replaced the lost `inject_registers.py`/`upgrade_condition.py` pair. Writing it surfaced the **two-conditions-on-one-line hazard** now recorded in the script-safety rules above.
- **2026-08-09 — Set 0 (Neurology) complete.** All 45 have detailed base + standard + brief + complications. Engine, `depth` audit of all 558 entries, and namespaced highlight spans shipped the same day.
- **2026-08-09 — Set 2 (Cardiovascular) STARTED, 16 of 63 done.** Three clusters complete: **Blood pressure** (2), **Coronary artery disease** (4) and **Heart failure & cardiomyopathies** (10). All 24 cardiology flowcharts preserved. 13 of the 16 needed detailed text written from scratch.
  - Registers for these 16: brief **112**, standard **298**, detailed **511** chars/field — see the drift note above; standard is the one off spec, and was corrected the same day.
  - Verified: balance 0/0/0, `node --check`, 558 unique ids, 0 duplicate top-level keys across all 558 objects, 0 field defects across the 16, and a live check of Hypertrophic cardiomyopathy showing 4 rows at Brief, 8 at Standard and 8 at Detailed, all authored.
- **2026-08-09 — Set 1 (Endocrinology) COMPLETE.** All 42 conditions now carry a detailed base, an authored `standard`, an authored `brief` and a `complications` row. Worked in four passes by cluster: Diabetes + Pituitary (10), Thyroid (10), Adrenal (8), then Parathyroid & bone + MEN + Electrolytes + Metabolic (14). 26 of the 42 had to have their detailed text written from scratch, because their base was at brief register; 5 were expanded from standard; 11 already had detailed text and needed only the shorter registers. All 20 existing flowcharts were preserved through the rewrite.
  - Registers achieved: **brief 103 chars/field**, **standard 239 over 7 fields**, **detailed 436**. Standard runs above the HFpEF reference of 192 partly because every entry now also carries Complications.
  - A second script was needed alongside `inject_registers.py`: **`upgrade_condition.py`** rewrites the base fields to detailed register, flips `depth`, and inserts standard/brief/complications while preserving `management`, `cluster`, `name` and `id` untouched. It carries existing `complications` through when the batch omits it, so good text is never silently overwritten.
  - Verified: balance 0/0/0, `node --check`, 558 unique ids, **0 duplicate top-level keys** across all 558 objects, 0 field defects across all 42 endocrine entries, and a live check of Addison's disease showing 8 rows at Detailed (381-char keyFacts), 8 at Standard (218) and 4 at Brief (100), all authored with no fallback markers.
